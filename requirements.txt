# Document RAG Search - Dependencies
# Install with: pip install -r requirements.txt

# Web server
flask>=3.0.0

# Anthropic Claude API
anthropic>=0.30.0

# PDF text extraction (fast, accurate)
pymupdf>=1.23.0

# Local embedding model
sentence-transformers>=2.7.0

# PyTorch (CPU version — change to cu118/cu121 if you have a GPU)
torch>=2.0.0

# Numerical operations (for vector search)
numpy>=1.24.0
